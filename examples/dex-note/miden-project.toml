[package]
name = "dex-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden:dex-note/miden-dex-note@0.1.0"
path = "src/lib.rs"

[dependencies]
miden-core = "*"
miden-protocol = "*"
basic-wallet = { path = "../basic-wallet" }

# The `midenc` namespace table groups compiler plugin configuration.
[package.metadata.midenc.note-codec]
crate = "../dex-note-codec"
