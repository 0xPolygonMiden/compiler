[package]
name = "miden-integration-tests-rust-fib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden-integration-tests-rust = { path = "../../rust" }
dlmalloc = { version = "0.2.4", features = ["global"]}

[profile.release]
lto = true
opt-level = "z"
