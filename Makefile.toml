[config]
default_to_workspace = false
skip_core_tasks = true
skip_git_env_info = true
modify_core_tasks = { private = true, namespace = "default" }
init_task = "init"

[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = true
CARGO_MAKE_CARGO_VERBOSE_FLAGS = { source = "${CARGO_MAKE_CI}", default_value = "", mapping = { "true" = "--verbose" } }
CARGO_MAKE_RUST_SCRIPT_PROVIDER = "rust-script"
CARGO_MAKE_USE_WORKSPACE_PROFILE = true
CARGO_MAKE_CARGO_BUILD_TEST_FLAGS = "--no-fail-fast"
CARGO_TARGET_DIR = { value = "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/target", condition = { env_not_set = [
    "CARGO_TARGET_DIR",
] } }
BACKTRACE_DEFAULT = { source = "${CARGO_MAKE_CI}", mapping = { "true" = "1", "false" = "0" } }
RUST_BACKTRACE = { value = "${BACKTRACE_DEFAULT}", condition = { env_not_set = [
    "RUST_BACKTRACE",
] } }
MIDENC_BIN_DIR = "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/bin"
MIDENC_INSTALL_DIR = "${MIDENC_BIN_DIR}/${CARGO_MAKE_RUST_TARGET_TRIPLE}"

# Workaround for https://github.com/0xMiden/miden-vm/issues/2025
RUST_MIN_STACK = 16777216

[env.development]
MIDENC_BUILD_PROFILE = "debug"

[env.production]
MIDENC_BUILD_PROFILE = "release"

[tasks.init]
script = ['''
#!@duckscript

# Set an environment variable containing the current rustup toolchain
#
if is_path_exists "rust-toolchain.toml"
    toolchain_file = readfile "rust-toolchain.toml"
    channel_start = indexof ${toolchain_file} "channel = "
    if not is_empty ${channel_start}
        channel_quoted_start = calc ${channel_start} + 11
        rest_of_file = substring ${toolchain_file} ${channel_quoted_start}

        channel_end = indexof ${rest_of_file} "\""
        if not is_empty ${channel_end}
            channel = substring ${rest_of_file} 0 ${channel_end}
            channel = trim ${channel}
            set_env CARGO_MAKE_RUSTUP_TOOLCHAIN_NAME ${channel}
        end
    end
end

cm_run_task "print-env"
''']

[tasks.default]
category = "Build"
description = "Default task builds the compiler"
alias = "build"

[tasks.print-env]
category = "Tools"
run_task = [
    { name = [
        "print-build-env",
        "print-ci-env",
        "print-rust-env",
        "print-cargo-env",
    ] },
]

[tasks.print-build-env]
private = true
script = ['''
#!@duckscript
echo "*************************************"
echo "Build Environment:"
echo "    Task: ${CARGO_MAKE_TASK}"
echo "    Task Arguments: ${CARGO_MAKE_TASK_ARGS}"
echo "    Command: ${CARGO_MAKE_COMMAND}"
echo "    Working Directory: ${CARGO_MAKE_WORKING_DIRECTORY}"
echo "    Target Directory: ${CARGO_TARGET_DIR}"
echo "    Profile: ${CARGO_MAKE_PROFILE}"
echo "    Bin Directory: ${MIDENC_BIN_DIR}"
echo "    Install Directory: ${MIDENC_INSTALL_DIR}"
echo "    Target Triple: ${CARGO_MAKE_RUST_TARGET_TRIPLE}"
echo "    RUST_BACKTRACE: ${RUST_BACKTRACE}"
echo "*************************************"
''']

[tasks.print-ci-env]
private = true
condition = { env = { "CARGO_MAKE_CI" = "true" } }
script = ['''
#!@duckscript
echo "*************************************"
echo "CI:"
echo "    CI: ${CARGO_MAKE_CI}"
echo "    PR: ${CARGO_MAKE_PR}"
echo "    Branch Name: ${CARGO_MAKE_CI_BRANCH_NAME}"
echo "    CI Vendor: ${CARGO_MAKE_CI_VENDOR}"
echo "*************************************"
''']

[tasks.print-rust-env]
category = "Tools"
condition = { env_set = ["CARGO_MAKE_RUST_CHANNEL"] }
script = ['''
#!@duckscript
output = exec --fail-on-error rustc "--version"
assert_eq ${output.code} 0
rustc_version_parts = split ${output.stdout} " "
hash_raw = array_get ${rustc_version_parts} 2
hash = replace ${hash_raw} "(" ""
date_raw = array_get ${rustc_version_parts} 3
date = replace ${date_raw} ")\n" ""
release ${rustc_version_parts}


echo "*************************************"
echo "Rust:"
echo "    Version: ${CARGO_MAKE_RUST_VERSION}"
echo "    Toolchain: ${CARGO_MAKE_RUSTUP_TOOLCHAIN_NAME}"
echo "    Channel: ${CARGO_MAKE_RUST_CHANNEL}"
echo "    Build Hash: ${hash}"
echo "    Build Date: ${date}"
echo "    Target Arch: ${CARGO_MAKE_RUST_TARGET_ARCH}"
echo "    Target Env: ${CARGO_MAKE_RUST_TARGET_ENV}"
echo "    Target OS: ${CARGO_MAKE_RUST_TARGET_OS}"
echo "    Pointer Width: ${CARGO_MAKE_RUST_TARGET_POINTER_WIDTH}"
echo "    Target Triple: ${CARGO_MAKE_RUST_TARGET_TRIPLE}"
echo "*************************************"
''']

[tasks.print-cargo-env]
category = "Tools"
condition = { env_set = ["CARGO_MAKE_CARGO_HOME"] }
script = ['''
#!@duckscript
echo "*************************************"
echo "Cargo:"
echo "    Home: ${CARGO_MAKE_CARGO_HOME}"
echo "    Profile: ${CARGO_MAKE_CARGO_PROFILE}"
echo "*************************************"
''']

[tasks.format]
category = "Development"
dependencies = ["format-rust"]

[tasks.check-format]
description = "Runs cargo fmt to check appropriate code format."
category = "Test"
command = "cargo"
args = ["fmt", "--", "--check"]
dependencies = ["install-rustfmt"]

[tasks.format-rust]
category = "Development"
description = "Formats source code (Rust)"
command = "cargo"
args = ["fmt"]
dependencies = ["install-rustfmt"]

[tasks.install-rustfmt]
category = "Development"
description = "Installs cargo rustfmt plugin."
env.CFG_RELEASE = { value = "${CARGO_MAKE_RUST_VERSION}", condition = { env_not_set = [
    "CFG_RELEASE",
] } }
env.CFG_RELEASE_CHANNEL = { value = "${CARGO_MAKE_RUST_CHANNEL}", condition = { env_not_set = [
    "CFG_RELEASE_CHANNEL",
] } }
install_crate = { crate_name = "rustfmt-nightly", rustup_component_name = "rustfmt", binary = "rustfmt", test_arg = "--help" }

[tasks.bloat]
category = "Development"
description = "Run cargo-bloat"
command = "cargo"
args = ["bloat", "${@}"]

[tasks.bench]
category = "Benchmarks"
description = "Benchmarks example package sizes and VM cycles, with SVG flamegraphs"
command = "cargo"
args = [
    "run",
    "@@remove-empty(CARGO_MAKE_CARGO_VERBOSE_FLAGS)",
    "-p",
    "midenc-benchmark-runner",
    "${@}",
]

[tasks.midenc]
category = "Build"
description = "Builds midenc and installs it to the bin folder"
command = "cargo"
args = [
    "-Z",
    "unstable-options",
    "build",
    "-p",
    "midenc",
    "--artifact-dir",
    "${MIDENC_BIN_DIR}",
]

[tasks.cargo-miden]
category = "Build"
description = "Builds cargo-miden and installs it to the bin folder"
command = "cargo"
args = [
    "-Z",
    "unstable-options",
    "build",
    "-p",
    "cargo-miden",
    "--artifact-dir",
    "${MIDENC_BIN_DIR}",
]

[tasks.hir-opt]
category = "Build"
description = "Builds hir-opt and installs it to the bin folder"
command = "cargo"
args = [
    "-Z",
    "unstable-options",
    "build",
    "-p",
    "midenc-hir-opt",
    "--artifact-dir",
    "${MIDENC_BIN_DIR}",
]

[tasks.miden-objtool]
category = "Build"
description = "Builds miden-objtool and installs it to the bin folder"
command = "cargo"
args = [
    "-Z",
    "unstable-options",
    "build",
    "-p",
    "miden-objtool",
    "--artifact-dir",
    "${MIDENC_BIN_DIR}",
]

[tasks.build]
category = "Build"
description = "Runs cargo build on the workspace"
run_task = [{ name = ["midenc"] }]

[tasks.install]
category = "Install"
description = "Installs the compiler via cargo"
run_task = [
    { name = [
       "install-midenc",
       "install-hir-opt",
       "install-cargo-miden",
       "install-miden-objtool",
    ] },
]

[tasks.check]
category = "Build"
description = "Runs cargo check on the workspace"
command = "cargo"
args = ["check", "${@}"]

[tasks.check-each]
category = "Build"
description = "Runs cargo check on each member of the workspace independently"
workspace = true
command = "cargo"
args = ["check", "${@}"]

[tasks.clean]
category = "Build"
description = "Clean build artifacts"
command = "cargo"
args = ["clean", "${@}"]

[tasks.sweep]
category = "Build"
description = "Garbage-collect the target directory: mark the live units by replaying the workspace build/test graphs, keep unmarked units younger than --age days (default 1; --cache-age 14 for directories the marks never reach), delete the rest; pass --dry-run to only report"
command = "python3"
args = [
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/sweep-target.py",
    "${CARGO_TARGET_DIR}",
    "${@}",
]

[tasks.sweep-deep]
category = "Build"
description = "Garbage-collect the target directory with no age guards: keep only the units the workspace build/test graphs mark live. Run after switching branch bases (a rebase orphans a whole build generation at once). Deletes IDE/clippy checker layers and the nested build caches; the next test run rebuilds them"
command = "python3"
args = [
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/sweep-target.py",
    "${CARGO_TARGET_DIR}",
    "--age",
    "0",
    "--cache-age",
    "0",
    "${@}",
]

# Runs at the end of `cargo make test-all`, the one flow after which the mark
# step compiles nothing (the whole graph was just built). Skips any directory a
# running build holds locked, and a failure (for example, no `python3` on the
# machine) never fails the flow. Set `MIDEN_SKIP_SWEEP` to disable it, for
# example when alternating between branches that supersede each other's builds.
[tasks.sweep-flow]
private = true
ignore_errors = true
condition = { env_not_set = ["MIDEN_SKIP_SWEEP"] }
command = "python3"
args = [
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/sweep-target.py",
    "${CARGO_TARGET_DIR}",
]

[tasks.test-sweep]
category = "Test"
description = "Run target-sweeper unit tests"
command = "python3"
args = [
    "-m",
    "unittest",
    "discover",
    "-s",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools",
    "-p",
    "test_sweep_target.py",
]

[tasks.update]
category = "Dependencies"
description = "Update dependencies to latest semver-compatible version"
command = "cargo"
args = ["update"]

[tasks.refresh-example-lockfiles]
category = "Dependencies"
description = "Refresh checked-in example and fixture lockfiles without compiling crates"
script = ['''
#!/usr/bin/env bash
set -euo pipefail

WORKSPACE="${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}"
refreshed=0

while IFS= read -r -d '' lockfile; do
    manifest="${WORKSPACE}/${lockfile%/Cargo.lock}/Cargo.toml"
    if [[ ! -f "${manifest}" ]]; then
        echo "missing manifest for ${lockfile}: ${manifest}" >&2
        exit 1
    fi

    echo "Refreshing ${lockfile}"
    cargo update --workspace --manifest-path "${manifest}"
    refreshed=$((refreshed + 1))
done < <(git -C "${WORKSPACE}" ls-files -z -- \
    'examples/**/Cargo.lock' \
    'tests/fixtures/**/Cargo.lock')

if [[ "${refreshed}" -eq 0 ]]; then
    echo "no checked-in example or fixture lockfiles found" >&2
    exit 0
fi

echo "Refreshed ${refreshed} lockfiles"
''']

[tasks.outdated]
category = "Dependencies"
description = "Look for dependencies with newer, possibly-incompatible versions"
command = "cargo"
args = ["outdated", "--workspace", "--root-deps-only"]

[tasks.check-deps]
category = "Dependencies"
description = "Check dependencies for lack of use, duplicates, vulnerabilities, etc."
run_task = [
    { name = [
        "unused",
        "duplicates",
        "audit",
    ] },
]

[tasks.unused]
category = "Dependencies"
description = "Check for unused dependencies"
command = "cargo"
args = ["shear"]

[tasks.duplicates]
category = "Dependencies"
description = "Check dependencies for multiple versions of the same crate"
command = "cargo"
args = ["tree", "--duplicate"]

[tasks.audit]
category = "Dependencies"
description = "Check dependencies for known vulnerabilities, deprecations"
command = "cargo"
args = ["audit"]

[tasks.test-all]
category = "Test"
description = "Runs the complete set of compiler tests"
run_task = { name = ["test-sweep", "test-rust", "test-lit"], fork = true, cleanup_task = "sweep-flow" }

[tasks.test]
category = "Test"
description = "Runs all Rust-based tests including network integration tests"
dependencies = ["test-sweep", "test-rust"]

[tasks.test-release]
category = "Test"
description = "Runs Rust-based tests via the workspace-wide test-release profile"
dependencies = ["test-release-rust"]

[tasks.install-cargo-miden]
category = "Install"
description = "Builds cargo-miden and installs it globally via the cargo bin directory"
command = "cargo"
args = [
    "install",
    "--locked",
    "--path",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/cargo-miden",
    "--${MIDENC_BUILD_PROFILE}",
    "--force",
    "--bin",
    "cargo-miden",
]

[tasks.install-midenc]
category = "Install"
description = "Builds midenc and installs it globally via the cargo bin directory"
command = "cargo"
args = [
    "install",
    "--locked",
    "--path",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/midenc",
    "--${MIDENC_BUILD_PROFILE}",
    "--force",
    "--bin",
    "midenc",
]

[tasks.install-hir-opt]
category = "Install"
description = "Builds hir-opt and installs it globally via the cargo bin directory"
command = "cargo"
args = [
    "install",
    "--locked",
    "--path",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/hir-opt",
    "--${MIDENC_BUILD_PROFILE}",
    "--force",
    "--bin",
    "hir-opt",
]

[tasks.install-miden-objtool]
category = "Install"
description = "Builds miden-objtool and installs it globally via the cargo bin directory"
command = "cargo"
args = [
    "install",
    "--locked",
    "--path",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tools/objtool",
    "--${MIDENC_BUILD_PROFILE}",
    "--force",
    "--bin",
    "miden-objtool",
]

[tasks.test-rust]
category = "Test"
description = "Run tests written in Rust, with optional extra arguments"
command = "cargo"
install_crate_args = ["--locked"]
args = [
    "nextest",
    "run",
    "@@remove-empty(CARGO_MAKE_CARGO_VERBOSE_FLAGS)",
    "@@split(CARGO_MAKE_CARGO_BUILD_TEST_FLAGS, )",
    "${@}",
]
dependencies = ["cargo-miden"]

[tasks.test-release-rust]
category = "Test"
description = "Run tests written in Rust via the workspace-wide test-release profile"
command = "cargo"
install_crate_args = ["--locked"]
args = [
    "nextest",
    "run",
    "--cargo-profile",
    "test-release",
    "@@remove-empty(CARGO_MAKE_CARGO_VERBOSE_FLAGS)",
    "@@split(CARGO_MAKE_CARGO_BUILD_TEST_FLAGS, )",
    "${@}",
]
dependencies = ["cargo-miden"]

[tasks.test-lit]
category = "Test"
description = "Runs the lit/filecheck test suite"
command = "litcheck"
env = { MIDENC_BIN_DIR = "${MIDENC_BIN_DIR}" }
args = [
    "lit",
    "run",
    "--verbose",
    "--path",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/bin",
    "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/tests/lit",
]
dependencies = ["litcheck", "midenc", "cargo-miden", "hir-opt", "miden-objtool"]

[tasks.lit]
category = "Test"
description = "Execute the lit utility"
command = "litcheck"
env = { MIDENC_BIN_DIR = "${MIDENC_BIN_DIR}" }
args = [
    "lit",
    "${@}",
]
dependencies = ["litcheck", "midenc", "cargo-miden", "hir-opt", "miden-objtool"]


[tasks.litcheck]
category = "Test"
description = "Set up the litcheck utility"
# 0.4.5 is the first release that supports --dump-input
install_crate = { crate_name = "litcheck", min_version = "0.4.6", test_arg = "--help" }

[tasks.docs]
category = "Documentation"
description = "Opens the public compiler documentation"
dependencies = ["docusaurus"]
cwd = "./docs/external"
command = "npm"
args = ["run", "start:dev"]

[tasks.rustdocs]
category = "Documentation"
command = "cargo"
args = ["doc"]


[tasks.docusaurus]
category = "Build"
cwd = "./docs/external"
command = "npm"
args = ["install"]

[tasks.clippy]
description = "Runs clippy on the workspace"
category = "Development"
command = "cargo"
args = [
    "clippy",
    "--all",
    "--all-targets",
    "${@}",
    "--",
    "-D",
    "clippy::all",
    "-D",
    "warnings",
]

[tasks.publish-dry-run]
category = "Test"
description = "Simulate publishing to uncover any build errors when each crate folder is built in isolation"
command = "cargo"
args = [
    "publish",
    "--dry-run",
]

[tasks.install-cargo-llvm-cov]
private = true
install_crate = { crate_name = "cargo-llvm-cov", binary = "cargo", test_arg = [
    "llvm-cov",
    "--version",
] }

[tasks.install-rustfilt]
private = true
install_crate = { crate_name = "rustfilt", binary = "rustfilt", test_arg = "--version" }

[tasks.fuzza-cov]
category = "Test"
description = "Run the differential tests under cargo-llvm-cov (clean slate) and emit a fuzza-oriented Markdown coverage report"
dependencies = ["install-cargo-llvm-cov", "install-rustfilt"]
script = ['''
#!/usr/bin/env bash
set -euo pipefail
WORKSPACE="${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}"
OUT="${CARGO_TARGET_DIR}/fuzza-coverage"
mkdir -p "${OUT}"

cargo llvm-cov clean --workspace
if [[ -f "${OUT}/report.json" ]]; then
    mv "${OUT}/report.json" "${OUT}/report.prev.json"
fi

cargo llvm-cov --package midenc-integration-tests --no-report -- differential --test-threads=1
cargo llvm-cov report --json --output-path "${OUT}/report.json"
cargo llvm-cov report --html --output-dir "${OUT}/html"

# Optionally scope the report to a target area. Set FUZZA_AREA to a
# comma-separated list of workspace-relative path prefixes (e.g.
# `codegen/masm/src/emit/mem.rs`) to add an area-focused section. The args
# array is always non-empty, so expansion is safe under `set -u`.
COV_ARGS=("${OUT}/report.json" "${WORKSPACE}" --prev "${OUT}/report.prev.json")
if [[ -n "${FUZZA_AREA:-}" ]]; then
    COV_ARGS+=(--area "${FUZZA_AREA}")
fi

python3 "${WORKSPACE}/tools/fuzza-agent/cov.py" "${COV_ARGS[@]}" > "${OUT}/report.md"

echo ""
echo "Coverage report written to:"
echo "  ${OUT}/report.md     (agent-consumable summary, includes delta if a prev snapshot existed)"
echo "  ${OUT}/html/         (human-readable browser view)"
echo "  ${OUT}/report.json   (raw llvm-cov JSON)"
''']

[tasks.fuzza-cov-step]
category = "Test"
description = "Incremental fuzza coverage run — reuses previous profile data and reports the delta against the last report"
dependencies = ["install-cargo-llvm-cov", "install-rustfilt"]
script = ['''
#!/usr/bin/env bash
set -euo pipefail
WORKSPACE="${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}"
OUT="${CARGO_TARGET_DIR}/fuzza-coverage"

if [[ ! -f "${OUT}/report.json" ]]; then
    echo "no baseline report at ${OUT}/report.json — run \`cargo make fuzza-cov\` first" >&2
    exit 1
fi

mv "${OUT}/report.json" "${OUT}/report.prev.json"

# `cargo llvm-cov` rejects `--no-report` together with `--no-clean`; we need
# `--no-clean` to preserve accumulated profile data across iterations, so we
# let it emit its default stdout report and then regenerate JSON/HTML from
# the merged profdata in the steps below. Tolerate test failures — a failed
# case still contributes coverage and the agent needs the delta even then.
test_status=0
cargo llvm-cov --no-clean --package midenc-integration-tests -- differential --test-threads=1 || test_status=$?
cargo llvm-cov report --json --output-path "${OUT}/report.json"
cargo llvm-cov report --html --output-dir "${OUT}/html"

# Optionally scope the report to a target area. Set FUZZA_AREA to a
# comma-separated list of workspace-relative path prefixes (e.g.
# `codegen/masm/src/emit/mem.rs`) to add an area-focused section. The args
# array is always non-empty, so expansion is safe under `set -u`.
COV_ARGS=("${OUT}/report.json" "${WORKSPACE}" --prev "${OUT}/report.prev.json")
if [[ -n "${FUZZA_AREA:-}" ]]; then
    COV_ARGS+=(--area "${FUZZA_AREA}")
fi

python3 "${WORKSPACE}/tools/fuzza-agent/cov.py" "${COV_ARGS[@]}" > "${OUT}/report.md"

echo ""
if [[ "${test_status}" -ne 0 ]]; then
    echo "NOTE: one or more differential tests failed; report reflects coverage up to the failures." >&2
fi
echo "Incremental coverage report with delta written to ${OUT}/report.md"
''']

[tasks.fuzza-cov-clean]
category = "Test"
description = "Clear accumulated fuzza coverage profile data and snapshot"
dependencies = ["install-cargo-llvm-cov"]
script = ['''
#!/usr/bin/env bash
set -euo pipefail
WORKSPACE="${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}"
cargo llvm-cov clean --workspace
rm -rf "${CARGO_TARGET_DIR}/fuzza-coverage"
''']

[tasks.fuzza-probe]
category = "Test"
description = "Cheap fuzza reachability probe: run one differential test outside llvm-cov with MIDENC_EMIT set, then list the freshly-written IR dumps. Usage: cargo make fuzza-probe <test-filter> [emit-kinds, default hir]"
script = ['''
#!/usr/bin/env bash
set -euo pipefail
WORKSPACE="${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}"
FILTER="${1:?usage: cargo make fuzza-probe <test-filter> [emit-kinds, default hir]}"
EMIT="${2:-hir}"
CASE="${FILTER##*::}"
OUT="${CARGO_TARGET_DIR}/fuzza-probe/${CASE}"
# Fresh output dir per probe so stale dumps can't be mistaken for this run's.
rm -rf "${OUT}"
mkdir -p "${OUT}"

# midenc runs in-process on every test invocation (cargo caching only affects
# the Rust->wasm step), but emit paths must be ABSOLUTE: bare kinds dump into
# the test process CWD (littering the source tree) and relative dirs vanish
# into the ephemeral cargo-build workspace. Rewrite each kind to kind=${OUT}.
SPEC=""
IFS=',' read -ra KINDS <<< "${EMIT}"
for k in "${KINDS[@]}"; do SPEC="${SPEC:+${SPEC},}${k}=${OUT}"; done

# Runs OUTSIDE cargo-llvm-cov on purpose: probes must not pollute the merged
# coverage profile with IR-printer regions, and the plain build stays warm
# across probes. --include-ignored lets a case wired with a temporary
# #[ignore] (the recommended probe setup) run without un-ignoring it.
status=0
MIDENC_EMIT="${SPEC}" \
    cargo test --package midenc-integration-tests "${FILTER}" -- \
    --include-ignored --test-threads=1 --nocapture || status=$?

# A probe of a divergent or compile-panicking case fails the test run; the
# emitted artifacts are still what the probe is for, so list them either way.
echo ""
echo "Emitted artifacts in ${OUT}:"
ls -l "${OUT}" 2>/dev/null || true
exit "${status}"
''']

[tasks.test-templates]
category = "Test"
description = "Scaffold a project from every template and build it, dev and release"
# An ordinary workspace member, so it shares the workspace's target directory,
# profile and lockfile with every other test. Each test generates a project and
# builds it, which takes minutes -- hence its own task and its own CI job rather
# than running inside the general test task.
dependencies = ["cargo-miden"]
command = "cargo"
args = ["make", "test", "-E", "package(midenc-template-tests)", "${@}"]

# Release tooling
# ============================================================
# Local, read-only or repository-local commands only. `publish` and `finalize`
# are deliberately absent: they cross irreversible boundaries and belong to the
# release workflow, where an approval gate and a short-lived token stand between
# them and production. A convenient local shortcut for them would be a way to
# skip that by accident.

[tasks.release]
category = "Release"
description = "Invoke a `release-tool` command"
command = "release-tool"
install_crate = { crate_name = "miden-release-tool", binary = "release-tool", test_arg = "--help" }
install_crate_args = ["--locked"]
args = [
    "${@}"
]
env = { "GITHUB_REPOSITORY" = "0xMiden/compiler" }
