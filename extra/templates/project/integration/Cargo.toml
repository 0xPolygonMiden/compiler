[package]
name = "integration"
version = "0.1.0"
edition.workspace = true

[dependencies]
# Pending a Miden client release compatible with protocol 0.17.0-rc.4.
miden-client = { version = "0.16.0-rc.1", features = ["tonic"] }
miden-client-sqlite-store = { version = "0.16.0-rc.1", package = "miden-client-sqlite-store" }
miden-standards = { version = "=0.17.0-rc.4", features = ["testing"] }
miden-testing = "=0.17.0-rc.4"
miden-mast-package = { version = "0.32.1", default-features = false }
tokio = { version = "1.48", features = ["rt-multi-thread", "net", "macros", "fs"] }
rand = { version = "0.10" }
anyhow = "1.0"
